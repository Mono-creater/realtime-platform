# realtime-platform 云服务器部署方案（v2，含摄像头视频/照片回传）

> 适用对象：`D:\realtime-platform\realtime-platform-main`（Vue3 + Express + Socket.IO + Prisma/MySQL + Modbus TCP 采集汇川 H5U PLC + 高清摄像头视频/照片回传）
> v2 变更：新增摄像头视频与照片接入方案（第三、四章），服务器选型因带宽/存储升级（第二章），部署脚本与成本同步更新
> v2.1 变更：新增第六章「启动指令速查」（一键启动脚本、日常启停、重启自检、本地开发启动）
> v2.2 变更：平台已在 Railway 部署——新增 8.1 现有部署分析与 8.2 迁云指引，规划"Railway=演示环境，云服务器=生产环境"双轨
> 编写日期：2026-09-01

---

## 一、先弄清楚：这个项目部署上云需要什么

分析代码后，这个平台的运行形态是：

| 组件 | 说明 | 部署含义 |
|---|---|---|
| Express 后端 | `server.js`，监听 3000 端口 | 一个 Node.js 进程 |
| 前端静态文件 | Vite 打包到 `dist/`，由 Express 直接托管 | **不需要单独的前端服务器**，一个服务全包 |
| Socket.IO | 实时推送 PLC 采集数据到浏览器 | 需要 WebSocket 支持（Nginx 反代时要配 Upgrade 头） |
| MySQL | Prisma schema 中 `provider = "mysql"` | 需要一个 MySQL 5.7/8.0 实例 |
| Modbus TCP 采集 | 连接 PLC（`PLC_HOST=192.168.1.88`，局域网地址） | 见第三章，云上需要隧道打通 |
| **视频回传** | 现场高清摄像头经 RTSP 回传直播流；前端 `SmartDetection/MonitorGlobal.vue` 已预留 `videoUrl` 槽位 | 云端需要一个**流媒体服务器**（第五章 ZLMediaKit），**带宽成为选型第一要素** |
| **照片回传** | 抓拍照片/现场照片经 HTTP 上传到后端 | 复用现有 `uploads/` + multer，量大后转对象存储（OSS/COS） |
| `uploads/` 目录 | multer 文件上传 | 需要持久化挂载，容器重建不丢 |
| 资源占用 | 采集周期 2 秒、单进程、数据量小 | 采集本身很轻；**视频流量和存储才是资源大头** |

项目自带 `Dockerfile`（node:22-alpine）、`railway.json`、`nixpacks.toml`，说明本来就为容器化部署准备的，走 Docker 路线最省事。

---

## 二、服务器选型（因视频/照片需求升级）

### 2.1 推荐结论

**首选：国内大厂的「轻量应用服务器」或同级云主机，4核4G~8G + 按流量计费带宽（峰值 5~10Mbps），Ubuntu 22.04/24.04。**

加了视频回传后，选型逻辑和纯数据采集时代完全不同，三个新结论：

1. **带宽取代 CPU/内存，成为第一瓶颈和花钱大头**。一路 1080P H.264 直播流约 2~4 Mbps，"固定 4~6Mbps 带宽"只够 1 个人看直播；而**按使用流量计费 + 高峰值带宽**的模式，平时不看不花钱，演示时才产生流量费，最适合你们这种"偶发观看"场景。
2. **录像原则上不放云端**。1080P 连续录像约 25~40GB/路/天，60GB 系统盘一天就爆。正确做法：**连续录像放现场 NVR/相机 SD 卡，云端只接收事件触发的抓拍照片和告警片段，直播按需拉流**。云端磁盘 60~100GB 足够。
3. **照片量小但会持续增长**，先用服务器磁盘 + 挂载 volume，接入规模化后（几万张以上）迁到对象存储 OSS/COS（约 0.1 元/GB/月，费用可忽略），数据库里只存 URL。

### 2.2 具体选购参数

| 项目 | 建议 |
|---|---|
| 厂商 | 阿里云 / 腾讯云 均可（腾讯云轻量 Lighthouse、阿里云轻量 SWAS），哪家有新用户优惠买哪家 |
| 机型 | 轻量应用服务器（流量包型套餐优先，如"峰值 8~12Mbps + 月流量包"）；纯采集+少量直播 2核4G 也够，**要转码（H.265→H.264）或多路并发请上 4核8G** |
| 磁盘 | 60~100GB SSD（云端不做连续录像，够用） |
| 操作系统 | **Ubuntu Server 22.04 LTS**（或 24.04），本文命令按 Ubuntu 写 |
| 地域 | 用户在国内 → 国内地域（如上海/广州/北京）；**不想备案域名 → 选香港** |
| 带宽模式 | **按流量计费、峰值 5~12Mbps**（推荐）；若固定带宽，至少 8Mbps 才能支撑 2~3 人同时看直播 |
| 数据库 | 不建议买云数据库 RDS。服务器上 Docker 跑 MySQL 容器即可，配合定时备份 |
| 对象存储（可选） | OSS/COS 存照片，约 0.1 元/GB/月，先用服务器磁盘、量大了再迁 |
| 域名 | 可选。演示用 IP 直接访问即可；正式用要买域名 + 国内节点需 ICP 备案（约 1~2 周），香港节点免备案 |

### 2.3 什么时候不选轻量服务器

- **平台目前已在 Railway 上运行**（构建配置分析见 8.1）。Railway 适合纯 simulation 演示，但接真实 PLC/摄像头、国内访问体验、视频流量成本三个问题上限明显，所以本文按 **"Railway 保留为演示环境，云服务器作为生产环境"** 规划，迁移步骤见 8.2。
- **以后要多路视频并发给大量观众看**：轻量服务器扛不住，需要 ECS/CVM + CDN 分发，现在没必要。

---

## 三、PLC 与摄像头的网络打通（本项目最特殊的环节）

PLC（`192.168.1.88:502`）和高清摄像头（RTSP，通常 `192.168.1.x:554`）都在**现场局域网**，云服务器在公网上够不到，需要打通。三种方式：

### 方案 A：纯模拟模式（最简单，适合演示/验收/毕设）

1. PLC：服务器上 `.env` 设 `PLC_MODE=simulation`，或把项目自带的 `plc-simulator.js` 也部署到云上（`PLC_HOST=127.0.0.1`、`PLC_PORT=1502`）。
2. 视频：本地准备一段 MP4，用 FFmpeg 循环推流到云端流媒体服务器（第五章有现成命令），冒充摄像头画面，整条视频链路都能演示。

### 方案 B：现场放网关，frp 一条隧道同时打通 PLC 和摄像头（推荐的真实方案）

架构：

```
云服务器(公网) ←─frp隧道─→ 现场小网关(工控机/旧电脑/N1盒子) ─→ 局域网 ─→ H5U PLC(502端口)
                                                              ─→ HD摄像头(554端口, RTSP)
```

- 现场那台小主机同时跑一个 frpc，**一个隧道配置映射多个端口**：PLC 的 502 + 每台摄像头的 554（多台相机映射成 7551、7552…不同本地端口）。
- 云端 `.env` 里 `PLC_HOST=127.0.0.1`、`PLC_PORT=6050`；流媒体服务器从 `rtsp://admin:密码@127.0.0.1:7551/...` 拉流，代码零改动。
- 或者用 **WireGuard** 组虚拟局域网（更安全、全流量加密），或商业方案蒲公英（图形化、小白友好）。

> ⚠️ **带宽硬约束**：现场出口的**上行**带宽必须 ≥ 所有视频码流之和 × 1.5（例如 2 路 1080P@4Mbps，现场上行至少 12Mbps），否则必卡顿花屏。现场是普通家宽的话，先测上行速度（speedtest）再定摄像头码率/数量，或让相机用 2~4Mbps 子码流。
>
> ⚠️ **安全警告**：Modbus 和 RTSP 都无加密鉴权，**绝对不要**把 502/554 端口直接暴露公网——必须走 VPN/隧道，且隧道口只对云服务器 IP 开放。

### 方案 C：摄像头走 GB28181 / 厂商云（适合多相机、点位分散）

- **GB28181**（国产监控相机大多支持国标）：云端部署 wvp-GB28181-pro + ZLMediaKit，**相机主动注册到云端**，天然穿 NAT、不需要现场网关隧道，4G/物联网相机也能用，多相机统一管理。适合相机数量多或现场没法放网关的情况。
- **厂商云直连**：萤石/乐橙等消费级相机走官方云 + EzUIKit 播放组件，服务器零带宽压力，但数据经第三方、按年付费，且画质/集成自由度受限。

**建议**：先用方案 A 把平台跑上云验证，接真实设备时 PLC+摄像头一起走方案 B 的 frp；相机多于 4 路或现场无法放网关，改用 GB28181。

---

## 四、视频与照片的接入方案（新增章节）

### 4.1 总体架构

```
现场摄像头(RTSP) ─frp隧道→ 云端 ZLMediaKit 流媒体服务器 ─HTTP-FLV/WebRTC→ 浏览器大屏播放
现场抓拍照片 ─HTTP POST→ Express 后端(multer) → uploads/ 卷 →（量大后）OSS/COS
MySQL 只存事件与资源 URL，不存二进制
```

### 4.2 直播视频：ZLMediaKit（推荐）

- **为什么选它**：Docker 一键部署，支持 RTSP/RTMP 拉流，自动转出 **HTTP-FLV（延迟 1~3 秒）/ WebRTC（亚秒）/ HLS（延迟 5~20 秒）** 供浏览器播放，带 REST API，中文文档全，工业监控场景的主流选择。（备选：SRS、mediamtx，能力类似。）
- **云端拉流**：后端或手动调 ZLM 的 `addStreamProxy` 接口，让它去拉隧道映射出来的相机 RTSP 地址；相机不推流它就不占带宽，**按需拉流**。
- **前端播放**：`MonitorGlobal.vue` 里预留的 `videoUrl` 直接填 ZLM 输出的播放地址即可。播放器推荐 **jessibuca**（国产、免费版支持 FLV/HLS、Canvas 解码，大屏兼容好）或 flv.js；要低延迟就用 WebRTC。Vue 里大概就是：

```js
// videoUrl 填法（由 Nginx 反代，见第五章）
videoUrl = 'http://你的域名/live/cam1.live.flv'   // HTTP-FLV，1~3s 延迟
videoUrl = 'http://你的域名/live/cam1/hls.m3u8'   // HLS，5~20s，适合回放
```

- **H.265 注意**：浏览器原生普遍不支持 H.265。要么把相机编码设为 **H.264**（子码流），要么前端用 jessibuca/WASM 硬解方案，要么云端 FFmpeg 转码（CPU 开销大，是"选 4核8G"的主要原因）。

### 4.3 照片回传：两条路

1. **HTTP 上传（推荐起步）**：现场程序/相机事件触发时，POST 到后端接口（项目已有 multer + `uploads/`），数据库记 URL 和事件关联。注意给接口加**鉴权和单文件大小限制**（如 10MB），`uploads/` 挂 volume 持久化。
2. **直传对象存储（规模化后）**：现场网关用 STS 临时凭证把照片直传 OSS/COS，云端只落库 URL。服务器磁盘和带宽都不受照片影响，是长期正解。

> 不推荐让相机 FTP 推文件到云端——明文协议走公网有安全隐患，且无法和业务事件关联。

### 4.4 录像与存储策略

| 内容 | 放哪里 | 理由 |
|---|---|---|
| 7×24 连续录像 | **现场 NVR / 相机 SD 卡** | 云端存储+流量成本是现场方案几十倍 |
| 告警事件片段/抓拍照片 | 云端 `uploads/`（或 OSS） | 量小、要和告警记录联动 |
| 直播 | ZLM 按需拉流，看完自动断 | 省流量 |

---

## 五、上云步骤（推荐路线：Docker Compose）

全程假设：已购买 Ubuntu 22.04 轻量服务器，能 SSH 登录（Windows 下用 `ssh root@服务器IP` 或 Windows Terminal）。

### 第 0 步：控制台防火墙放行端口

放行：`22`（SSH）、`80`、`443`；使用 WebRTC 低延迟直播时另放行 `UDP 10000`。
**不要**放行 3000、3306、502、554——它们只应通过 Nginx 或隧道访问。

### 第 1 步：安装 Docker（服务器上执行）

```bash
# 官方一键脚本（国内网络慢时用阿里云镜像源版）
curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun

# 启动并设置开机自启
systemctl enable --now docker

# 验证
docker --version && docker compose version
```

国内拉镜像慢，给 Docker 配置镜像加速（`/etc/docker/daemon.json`）：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://dockerproxy.net"
  ]
}
```

改完执行 `systemctl restart docker` 生效。

### 第 2 步：上传代码到服务器

```bash
# 方式1：git（推荐，以后更新方便）
apt update && apt install -y git
git clone <你的仓库地址> /opt/realtime-platform

# 方式2：本机 scp 直接传（在 Windows PowerShell 里执行）
scp -r "D:\realtime-platform\realtime-platform-main" root@服务器IP:/opt/realtime-platform
```

> 注意：上传前把 `.env` 改成生产值（见第 3 步），`node_modules` 不用传（容器内重新安装）。

### 第 3 步：编写 `docker-compose.yml`（放在项目根目录）

项目自带的 Dockerfile 缺了 Prisma 生成步骤和时区，建议按第六节"改进版 Dockerfile"替换。然后创建 `docker-compose.yml`：

```yaml
services:
  mysql:
    image: mysql:8.0
    container_name: rtp-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 改成强密码
      MYSQL_DATABASE: realtime_platform
      MYSQL_USER: rtp
      MYSQL_PASSWORD: 改成强密码
      TZ: Asia/Shanghai
    volumes:
      - mysql-data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-p改成强密码"]
      interval: 5s
      retries: 20

  app:
    build: .
    container_name: rtp-app
    restart: always
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      TZ: Asia/Shanghai
      # Prisma 连接串：host 写服务名 mysql，不是 localhost
      DATABASE_URL: mysql://rtp:改成强密码@mysql:3306/realtime_platform
      # ===== PLC 配置，按第三章方案选择 =====
      PLC_MODE: real                # simulation=纯模拟；real=采集
      PLC_HOST: 127.0.0.1           # frp 穿透后 frps 监听在宿主机，填映射端口
      PLC_PORT: 6050                # frp 把现场 PLC 502 映射到宿主机 6050
      PLC_UNIT_ID: 1
      PLC_INTERVAL: 2000
      # ===== 流媒体服务器（ZLMediaKit）地址，供后端调 API 拉流 =====
      ZLM_API_URL: http://zlm:80
      ZLM_SECRET: 改成config里的secret
    ports:
      - "127.0.0.1:3000:3000"       # 只绑回环，由 Nginx 对外，3000 不暴露公网
    volumes:
      - ./uploads:/app/uploads      # 照片/文件持久化

  plc-sim:                          # 方案A：PLC 模拟器跑在云上；不需要可整段删除
    build: .
    container_name: rtp-plc-sim
    restart: always
    command: node plc-simulator.js
    environment:
      PLC_SIM_HOST: 0.0.0.0
      PLC_SIM_PORT: 1502
      PLC_SIM_UNIT: 1

  zlm:                              # ===== 流媒体服务器：接收摄像头流、输出网页可播格式 =====
    image: zlmediakit/zlmediakit:master
    container_name: rtp-zlm
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"   # 让容器能访问宿主机上的 frp 隧道端口
    ports:
      - "554:554"                   # RTSP（仅当相机主动推流时需要；云端主动拉流可不映射）
      - "127.0.0.1:8080:80"         # HTTP API + HTTP-FLV/HLS 播放，只绑回环走 Nginx
      - "11935:1935"                # RTMP，可选
      - "10000:10000/udp"           # RTP/WebRTC 收发流，公网必须可达
      - "8000:8000/udp"             # GB28181 信令收流，走方案C时才需要
    volumes:
      - ./zlmediakit/config.ini:/opt/media/conf/config.ini   # 官方仓库拷默认配置后改 secret
      - zlm-data:/opt/media/bin/www                           # 录制/截图持久化

volumes:
  mysql-data:
  zlm-data:
```

frp 侧（现场网关 frpc.toml 示例，云端 frps 略，网上教程多）：

```toml
[[proxies]]
name = "plc-modbus"
type = "tcp"
localIP = "192.168.1.88"      # 现场 PLC
localPort = 502
remotePort = 6050             # 云服务器上访问 127.0.0.1:6050 即 = PLC

[[proxies]]
name = "camera-1-rtsp"
type = "tcp"
localIP = "192.168.1.64"      # 现场 1 号摄像头
localPort = 554
remotePort = 7551             # 云上 RTSP 地址变为 rtsp://admin:密码@host:7551/...
```

### 第 4 步：初始化数据库并启动

```bash
cd /opt/realtime-platform

# 构建镜像 + 拉起 MySQL（首次较慢）
docker compose up -d mysql

# 等 MySQL 健康后，执行 Prisma 迁移（项目已带 prisma/migrations）
docker compose run --rm app npx prisma migrate deploy
# 如果报没有迁移记录，改用：docker compose run --rm app npx prisma db push

# 启动全部服务（app + zlm，含/不含 plc-sim 按需）
docker compose up -d

# 验证
docker compose ps                          # 各容器 running / healthy
curl -I http://127.0.0.1:3000              # HTTP 200 即平台正常
curl -s "http://127.0.0.1:8080/index/api/getServerConfig?secret=你的secret" | head -c 200
```

### 第 5 步：验证视频链路（先模拟后真实）

**模拟冒烟测试**——服务器上找/传一个 MP4，用 FFmpeg 推给 ZLM：

```bash
docker compose exec zlm ffmpeg -re -stream_loop -1 -i /opt/media/bin/www/test.mp4 \
  -c:v libx264 -preset veryfast -tune zerolatency -g 50 -r 25 \
  -c:a aac -f flv rtmp://127.0.0.1:1935/live/cam1
```

浏览器访问 `http://服务器IP/live/cam1.live.flv`（配好 Nginx 后）能出画面，说明整条播放链路通。之后把推流源换成真实相机即可：

- frp 隧道方案：调 ZLM API 拉流——
  `curl "http://127.0.0.1:8080/index/api/addStreamProxy?secret=你的secret&vhost=__defaultVhost__&app=live&stream=cam1&url=rtsp://admin:相机密码@host.docker.internal:7551/Streaming/Channels/101"`
  （URL 里的相机 RTSP 路径各厂商不同，海康如上、大华 `/cam/realmonitor?channel=1&subtype=0`，以相机说明书为准）
- GB28181 方案：相机里把 SIP 服务器指到云服务器 IP:5060，在 wvp 管理页点播。

**照片链路**：现场用 `curl -F "file=@snap.jpg" -H "Authorization: ..." http://服务器IP/api/你的上传接口` 试传一张，确认 `uploads/` 出现文件且数据库有记录。

### 第 6 步：Nginx 反向代理 + HTTPS（对外提供服务）

```bash
apt install -y nginx certbot python3-certbot-nginx
```

新建 `/etc/nginx/sites-available/realtime`：

```nginx
server {
    listen 80;
    server_name 你的域名或服务器IP;

    # 平台 + Socket.IO
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        # Socket.IO WebSocket 升级必需的两行
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 300s;
    }

    # 流媒体播放（HTTP-FLV / HLS / WebRTC 信令），转发给 ZLMediaKit
    location /live/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;    # WebRTC 播放也要升级头
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        flv_live_streaming off;                    # 非必需项，报错可删
    }
}
```

> 注：`flv_live_streaming` 不是 Nginx 标准指令，报错就删掉那行；核心是 `/live/` 反代到 8080 且带 Upgrade 头。

启用并签发 HTTPS 证书（有域名时）：

```bash
ln -s /etc/nginx/sites-available/realtime /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
certbot --nginx -d 你的域名       # 自动配好 443 和证书续期
```

**完成。** 浏览器打开 `http://服务器IP`：大屏正常、数据在动、`SmartDetection` 页 `videoUrl` 填入 `/live/cam1.live.flv` 后能看直播，即部署成功。

### 第 7 步：日常运维速查

```bash
docker compose logs -f app        # 看应用日志
docker compose logs -f zlm        # 看流媒体日志（拉流失败先看这里）
docker compose restart app        # 重启应用
docker compose up -d --build app  # 更新代码后重新发布
docker compose down               # 全部停止（数据在 volume 里不会丢）
df -h && du -sh uploads/ zlmediakit卷   # 盯磁盘，视频/图片是增长源
```

MySQL 定时备份（`crontab -e` 加一行，每天凌晨 3 点）：

```cron
0 3 * * * docker exec rtp-mysql sh -c 'exec mysqldump -uroot -p"你的密码" realtime_platform' | gzip > /opt/backup/rtp_$(date +\%F).sql.gz
```

---

## 六、启动指令速查（v2.1 新增）

> 首次部署按第五章完整流程走一遍；之后日常开机、发布、恢复，直接复制本章命令。
> 所有命令默认在项目目录执行：`cd /opt/realtime-platform`。

### 6.1 一键启动脚本（推荐）

把下面内容保存为项目根目录 `start.sh`：

```bash
#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"

echo "==> 1/4 启动 MySQL 并等待健康"
docker compose up -d mysql
docker compose up -d --wait mysql

echo "==> 2/4 同步数据库结构（幂等，已同步则自动跳过）"
docker compose run --rm app npx prisma migrate deploy

echo "==> 3/4 启动全部服务（平台 + 流媒体 + PLC 模拟器）"
docker compose up -d --build

echo "==> 4/4 健康检查"
sleep 3
curl -sf -o /dev/null http://127.0.0.1:3000 \
  && echo "✅ 平台正常，浏览器访问 http://服务器IP" \
  || echo "❌ 平台异常，查日志：docker compose logs -f app"
docker compose ps
```

首次使用（Windows 上编辑过的脚本先处理换行符，见坑 15）：

```bash
sed -i 's/\r$//' start.sh     # Windows 记事本编辑过才需要；或 apt install -y dos2unix && dos2unix start.sh
chmod +x start.sh
./start.sh
```

### 6.2 日常启停（按场景复制）

```bash
# ---- 启动 ----
docker compose up -d                    # 后台启动全部服务
docker compose up -d --build            # 代码更新后重新构建并启动

# ---- 停止 ----
docker compose down                     # 停止并移除容器（数据在卷里，不会丢）
docker compose stop                     # 只停止不删除，下次 start 秒起

# ---- 重启 ----
docker compose restart                  # 重启全部
docker compose restart app              # 只重启平台后端
docker compose restart zlm              # 只重启流媒体

# ---- 单独启停某个服务 ----
docker compose up -d app                # 只启动平台
docker compose up -d zlm                # 只启动流媒体
docker compose up -d plc-sim            # 只启动 PLC 模拟器
docker compose stop plc-sim             # 接真实 PLC/摄像头后，停掉模拟器
```

### 6.3 服务器重启后（云主机维护/宕机恢复）

正常情况**无需任何操作**：Docker 已设自启（`systemctl enable docker`）+ 各容器 `restart: always`，开机后会自动全部拉起。若重启 5~10 分钟后仍访问不了，按顺序排查：

```bash
systemctl status docker                 # 1. Docker 本身是否在运行
docker compose ps                       # 2. 哪个容器没起来（看 STATUS）
docker compose up -d                    # 3. 手动拉起没起来的服务
docker compose logs -f --tail=50 app    # 4. 还不行就看应用日志
curl -I http://127.0.0.1:3000           # 5. 容器内验证：HTTP 200 即恢复
```

### 6.4 前端改动后的重新发布（含 SmartDetection 大屏/videoUrl 页面）

```powershell
# ① 本地 Windows PowerShell：重新打包
cd D:\realtime-platform\realtime-platform-main
npm run build

# ② 只上传 dist 目录
scp -r "D:\realtime-platform\realtime-platform-main\dist" root@服务器IP:/opt/realtime-platform/
```

```bash
# ③ 服务器上：静态文件由 Express 托管，重启即生效
docker compose restart app
```

### 6.5 前后端启动指令（本地环境）

> 本项目是"前后端一体"架构：前端（Vue3）用 Vite 打包成 `dist/`，由后端 Express（`server.js`）直接托管。**生产环境只需启动后端一个进程，前端不需要单独启动**；只有开发模式才前后端分开跑。

**首次准备（只做一次）**：

```bash
npm install        # 安装依赖（Node 版本要求 20.19+ 或 22.12+）
```

**方式一：开发模式（前后端分开跑，改代码实时生效）**

```bash
# 终端1 —— 启动后端（Express + Socket.IO + PLC 采集，端口 3000）
npm start          # 等价于 node server.js

# 终端2 —— 启动前端（Vite 开发服务器，端口 5173，热更新）
npm run dev
```

浏览器访问 `http://localhost:5173`。Vite 已配置把 `/api` 和 `/socket.io` 自动代理到 3000，**必须先启动后端再启动前端**；需要 PLC 数据时再开一个终端跑 `npm run plc-sim`（模拟器，端口 1502）。

**方式二：生产方式（本地验证上云效果，一条命令跑完前后端）**

```bash
npm run build      # 打包前端到 dist/（每次改前端代码后需重新执行）
npm start          # 启动服务：后端 API + Socket.IO + 前端页面，统一入口 http://localhost:3000
```

**云服务器上**：不用上述命令，直接执行 6.1 的 `./start.sh` 或 `docker compose up -d --build`（依赖安装在镜像构建时自动完成，前端改动按 6.4 重新打包上传 dist 后 `docker compose restart app`）。

---

## 七、附：改进版 Dockerfile（建议替换项目根目录同名文件）

原 Dockerfile 可以跑，但有三个小问题：没显式 `prisma generate`、没设时区、上下文太大。改进版：

```dockerfile
FROM node:22-alpine
WORKDIR /app
ENV TZ=Asia/Shanghai
RUN apk add --no-cache tzdata

COPY package*.json ./
COPY prisma ./prisma
RUN npm install && npx prisma generate

COPY . .

EXPOSE 3000
CMD ["node", "server.js"]
```

> `dist/` 已在仓库中，直接由 Express 托管。若改了前端代码，先在本地跑 `npm run build` 再上传（或以后在 CI 里自动构建）。
> 若需要云端转码 H.265，另建一个带 ffmpeg 的 sidecar 容器或换 `node:22` 基础镜像加 `apt install ffmpeg`，一般用不到。

---

## 八、Railway 现有部署分析与迁云指引（含备选路线）

### 8.1 现有 Railway 部署是什么状态

项目根目录的 `railway.json`（builder=NIXPACKS）+ `nixpacks.toml` 定义了 Railway 的构建方式：

```toml
[build]
builder = "nodejs"
buildCommand = "npm install"     # 只装依赖，不执行 vite build —— 用的是仓库里已提交的 dist/

[nodejs]
version = "22"

[deploy]
startCommand = "node server.js"  # 前后端一体启动，Railway 自动注入 PORT 环境变量
```

也就是说，Railway 上跑的就是 **Node 22 + `node server.js` 的前后端单体**，和云服务器 Docker 方案本质相同，前端同样吃仓库里已打包好的 `dist/`。当前能力边界：

| 维度 | Railway 现状 | 说明 |
|---|---|---|
| PLC / 摄像头 | ❌ 连不了现场真实设备 | Railway 上无法自建 frp 服务端、UDP 受限（WebRTC/GB28181 收流不可行），只能 `PLC_MODE=simulation` |
| 国内访问 | ⚠️ 延迟 200ms+、偶尔不稳 | 节点在海外，大屏与 Socket.IO 实时体验打折 |
| 视频 | ❌ 基本不可用 | 端口与带宽受限，且按用量计费流量费高 |
| 数据库 | 取决于是否加了 MySQL 插件 | `server.js` 检测不到 `DATABASE_URL` 会自动以**无数据库模式**运行（告警历史、用户登录等功能不落库） |
| 成本 | 按 CPU/内存/流量用量计费 | 空闲演示很便宜，上视频后费用飙升 |

**结论：Railway 继续当"纯模拟在线 Demo"没问题；生产环境（真实 PLC + 摄像头 + 国内用户）按本文第一~七章迁到云服务器。**

### 8.2 迁云三步：导数据 → 迁配置 → 切流量

**① 导出 Railway 数据库（仅当加了 MySQL 插件且要保留告警历史/用户数据）**

```bash
# 用 Railway 控制台里 MySQL 服务的公网代理地址，在本地导出
mysqldump -h <Railway MySQL 公网代理主机> -P <端口> -u root -p \
  --single-transaction realtime_platform > railway_backup.sql

# 云服务器上导入
docker exec -i rtp-mysql sh -c 'exec mysql -uroot -p"你的密码" realtime_platform' < railway_backup.sql
```

> 演示数据无所谓的话跳过这步，直接靠第 4 步的 `prisma migrate deploy` 建空库。

**② 迁移配置**：把 Railway 控制台 Variables 里的环境变量抄到云服务器的 `.env` / `docker-compose.yml`（重点：`DATABASE_URL` 的 host 改成 `mysql` 服务名、`PLC_*` 系列按第三章调整），然后按第五章部署、第六章 `./start.sh` 启动。

**③ 切换访问入口**：

- 用 IP 访问：无切换动作，把新地址发出去即可；
- 有域名：把 DNS 解析从 Railway 分配的地址改指云服务器 IP——Railway 环境不用停，解析改回来随时回退；
- **建议双环境并存**：Railway 保持 simulation 模式当线上 Demo，云服务器跑生产，互为备份、互不影响。

### 8.3 其他备选路线（按情况选）

| 路线 | 适合谁 | 要点 |
|---|---|---|
| **Railway（现状保留）** | 纯 simulation 演示、海外访问 | 已在运行，见 8.1；接真实设备/国内用户/视频请按 8.2 迁云 |
| **宝塔面板** | 完全不想碰命令行 | 装宝塔 → 软件商店装 Node + MySQL + Nginx → 上传代码 → PM2 启动 `server.js` → Nginx 反代 3000。视频部分仍建议 Docker 跑 ZLMediaKit |
| **裸机 Node + PM2** | 想学原生部署 | NodeSource 装 Node 22 → apt 装 MySQL → `npm install && npx prisma migrate deploy` → `pm2 start server.js && pm2 startup` |
| **萤石云等厂商云 + EzUIKit** | 用消费级相机、不想自己搞流媒体 | 相机注册厂商云，前端嵌官方播放组件，服务器零视频带宽压力；数据经第三方，按年付费 |

---

## 九、成本估算（v2：视频是新增变量）

| 项目 | 费用（参考，以官网活动为准） |
|---|---|
| 服务器 4核4G~8G，按流量计费 | 约 300~800 元/年（新用户活动价） |
| Railway 演示环境（可选保留） | 约 $5/月起（Hobby 档按用量计费，纯 simulation 用量很小） |
| 按流量费 | 1080P 直播 1 小时 ≈ 1.4GB ≈ 1 元/GB 计；每天看 1 小时，一月约 30~50 元；不看直播几乎为 0 |
| 域名（.com/.cn） | 约 30~80 元/年（可选） |
| SSL 证书 | 0 元（Let's Encrypt 免费自动续期） |
| MySQL / frp / ZLMediaKit | 0 元（全部 Docker 自建） |
| 照片存储 | 服务器磁盘内约 0 元；转 OSS 后 1000 张×3MB ≈ 0.3 元/月 |
| 现场网关（frp 方案） | 0~200 元（旧电脑或 N1 盒子）+ 现场宽带需上行 ≥ 视频码流×1.5 |
| **合计** | **一年约 500~1200 元（取决于看直播的时长）** |

---

## 十、常见坑清单（v2 增加视频相关）

1. **Prisma 连接不上 MySQL**：compose 里 `DATABASE_URL` 的 host 必须写服务名 `mysql`，不是 `localhost`。
2. **Socket.IO 页面连不上/数据不动**：Nginx 漏配 `Upgrade`/`Connection` 两行 WebSocket 头。
3. **时间差 8 小时**：容器和 MySQL 都加 `TZ=Asia/Shanghai`。
4. **重启后服务没起来**：compose 已配 `restart: always`，Docker 本身 `systemctl enable docker` 要开自启。
5. **`.env` 不要提交进 git**：服务器单独维护生产版；相机密码、数据库密码全改强密码。
6. **3000/3306/502/554 千万别在防火墙放行**：对外只留 80/443（WebRTC 另加 UDP 10000）。
7. **ZLM 容器访问宿主机的 frp 隧道端口不通**：必须给 zlm 服务加 `extra_hosts: host.docker.internal:host-gateway`（Linux 默认没有这个域名），拉流 URL 写 `host.docker.internal:7551` 而不是 `127.0.0.1`。
8. **视频黑屏但有声音/花屏**：相机出的是 H.265，浏览器放不出 → 相机改 H.264 子码流，或上转码（这就是选 4核8G 的原因）。
9. **直播延迟太高**：HLS 本身 5~20 秒延迟，实时监控请用 HTTP-FLV（1~3 秒）或 WebRTC（亚秒）。
10. **直播卡顿花屏**：先测现场出口**上行**带宽是否 ≥ 码流总和×1.5，这是最常见的瓶颈，不是云服务器的问题。
11. **磁盘被吃满**：云端不做 7×24 连续录像；ZLM 的录制目录（zlm-data 卷）要定期清理，`df -h` 盯磁盘。
12. **照片接口被刷**：上传接口必须加鉴权和文件大小/类型校验（multer limits + 白名单后缀）。
13. **镜像拉取超时**：配置第 1 步的 registry-mirrors 镜像加速。
14. **真实设备连不上**：PLC 在云上 `docker exec -it rtp-app sh` 后 `nc -zv 127.0.0.1 6050` 测隧道；视频看 `docker compose logs -f zlm` 里拉流报错（多为相机密码/RTSP 路径错）。
15. **`start.sh` 报 `bash\r: bad interpreter`**：在 Windows 上编辑过的脚本带 CRLF 换行符，上传后执行 `sed -i 's/\r$//' start.sh`（或 `dos2unix start.sh`）再跑。
